# Archived redesign mockups (April 2026)

This snapshot contains the exploratory **HTML/CSS mockups** and design notes for a proposed chrome consolidation (top bar / copy placement / Lucide toolbar icons / `state-model` + `chrome-design` docs).

**Archived path:** `redesign/` (moved from `mockups/redesign/`).

**Starting fresh:** new mockups should be derived from **the shipping UI**, primarily:

- `standalone/index.html` — structure of header, toolbars, workspace
- `media/style.css` + `media/webview.css` — authored styles consumed by standalone and the extension webview

The archived references to `mockups/redesign/...` inside these files apply only within this archive; update paths if you resurrect any file verbatim.
